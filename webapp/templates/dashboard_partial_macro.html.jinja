{% macro list(label, items) %}

{% if items %}
<div class="d-flex flex-column">
  <a>{{ label }}:</a>
  <ul class="mb-0">
    {% for item in items %}
    <li>{{ item }}</li>
    {% endfor %}
  </ul>
</div>
{% endif %}

{%- endmacro %}
